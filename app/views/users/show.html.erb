

<% if @user.errors.any? %>
  <h2 class="text-danger">
    <%= @user.errors.count %>error prohibited this obj from being saved:
  </h2><br>
  <ul class="text-danger">
    <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
<% end %>
  </ul>
      
<main><div class='container px-5 px-sm-0'>
    <%= render 'users/list', user: @user, book: @book %>

    <div class="row">
      <div class='col-md-8 offset-md-1'>
        <h2 class="border-bottom" style="padding10px;">Books</h2>
        <div class="row justify-content-md-center">
          <div class="col col-lg-2">Title</div>
          <div class="col-md-auto">Opinion</div>
          <div class="col col-lg-2",colspan="3"></th>
          
        <div class="row">
        <% @books.each do |book| %>
          <div class="col">
          <p><%= attachment_image_tag book.user, :profile_image, :fill, 30, 30, fallback: "no_image.jpg" %></p>
          </div>
          <div class="col-md-auto">
            <%= link_to book.title, book_path(book.id) %>
          </div>
          <div class="col col-lg-2">
            <span><%= book.body %></span>
          </div>
        <% end %>
        </div>
        <%= paginate @books %>
      </div>
    </div>
  </div>
</div>
</main>


